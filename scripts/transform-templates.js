#!/usr/bin/env node
// Script rendu inopérant pour préserver la nouvelle syntaxe @if/@for/@let native.
// Les transformations sont désormais gérées nativement par Angular 17+.
// Si besoin de réactiver, restaurer le code original depuis git.

console.log('Transform-templates script is disabled to preserve native @if/@for/@let syntax.');
process.exit(0);
