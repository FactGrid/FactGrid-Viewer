// DEPRECATED: This file was used by Karma/Jasmine. The project now uses Vitest.
// See `src/test-setup.ts` and `vitest.config.ts` for the current test setup.
// Keeping this file for backward compatibility (so old tooling doesn't fail),
// but it should no longer be necessary.

// NOTE: Do not import or re-initialize the TestBed here when using Vitest.
// The current test runner will use `src/test-setup.ts` configured in `vitest.config.ts`.
